import React from "react";
import { Image, ImageProps } from "@mantine/core";

/**
 * Image widget component.
 *
 * @returns
 */

export default function ImageWidgetComponent(props: ImageProps) {
	return <Image {...props}/>;
}
