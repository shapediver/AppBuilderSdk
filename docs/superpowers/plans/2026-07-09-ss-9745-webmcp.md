# SS-9745 WebMCP Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Register four WebMCP imperative-API tools on App Builder so external AI agents can list parameters, set values, and create/import model states; reuse schemas for a future in-app agent framework.

**Architecture:** New FSD slice `src/shared/features/webmcp` in AppBuilderShared submodule. `useWebMcpTools` registers tools on `document.modelContext` (fallback `navigator.modelContext`) after session + parameters are ready. Zod `strictObject` schemas → hand-rolled `zodToJsonSchema` → `registerTool`. Set/list logic is unit-tested headless; create/import roundtrip deferred to subagent QA (schema-only in `eval:webmcp`).

**Tech Stack:** React 19, TypeScript, Zod 4, Zustand, Vite, Jest, Chrome WebMCP origin trial, ShapeDiver session API.

**Status (2026-07-17):** **SS-9745 scope complete.** 4 tools wired, **46 unit tests** (`parametersFilter` + `webmcp`) + 25 headless evals pass, weak-model QA on SS-8076/SS-8557. Post-review hardening (Task 8) + PR #334 follow-ups (Task 9) done. Canonical create/import Zod in `model-state`. Layout `getParameterRefs` left as on `development` (in-app agent only — not part of WebMCP). Follow-ups deferred (see end).

**PR:** [AppBuilderShared #334](https://github.com/shapediver/AppBuilderShared/pull/334) — `CHANGES_REQUESTED` addressed in code (replies deferred by user).

**Jira:** SS-9745

**Branch:** `task/SS-9745` (parent + `src/shared` submodule)

**Manual test URLs:**
- Float sliders: `http://localhost:3000/?g=SS-8557.json`
- StringList / Color trap: `http://localhost:3000/?g=SS-8076.json`

**Reference:** [WebMCP imperative API](https://developer.chrome.com/docs/ai/webmcp/imperative-api)

---

## File map

### Submodule `src/shared/features/webmcp/`

| File | Action | Status |
|------|--------|--------|
| `config/tools.ts` | Tool names + weak-model-friendly descriptions | Done |
| `config/listParameterDefinitions.ts` | Zod strictObject + `.describe()` | Done |
| `config/setParameterValues.ts` | Zod schemas + types only | Done |
| `lib/resolveSetParameterUpdates.ts` | `resolveAndUpdate`, batch feedback | Done |
| `lib/setParameterValueValidators/prepareParameterStoreValue.ts` | Shape agent input → `parameter.actions.isValid` + agent-friendly error messages from `definition` metadata; canonical `isValid(value, true)` fallback for unknown types | Done |
| `lib/setParameterValueValidators/types.ts` | `ParameterValueInput` type | Done |
| `model/tools/register*Tool.ts` | Per-tool registration (4 files) | Done |
| `model/webMcpToolsDeps.ts` | Shared refs for tool registration | Done |
| `config/createModelState.ts` | Input = `createModelStateDataSchema.omit({ image })` (describes on canonical Zod) | Done |
| `config/importModelState.ts` | Input/output schema incl. `invalidParameters` | Done |
| `lib/zodToJsonSchema.ts` | Hand-rolled JSON Schema; `additionalProperties: false` | Done |
| `lib/parameterDefinitionMapper.ts` | `IShapeDiverParameter` → list output | Done |
| `lib/stringListValue.ts` | String index for ShapeDiver `isValid` | Done |
| `lib/formatToolInputError.ts` | Unified parse-error shape for list/set | Done |
| `lib/computeAppliedParameterIds.ts` | Import diff helper | Done |
| `lib/webmcpAvailability.ts` | `modelContext` + `getWebMcpEnvironment()` / `ready` | Done |
| `model/useWebMcpTools.ts` | Register 4 tools, AbortController lifecycle | Done |
| `model/useWebMcpTools.types.ts` | `registered`, `ready`, `environment` | Done |
| `evals/evals.json` | 25 headless scenarios | Done |
| `evals/runWebmcpEvals.ts` | Eval runner | Done |
| `evals/__fixtures__/parameters.ts` | Mock params; per-param `isValid` overrides only where needed | Done |
| `__tests__/*.test.ts` | 6 webmcp suites | Done |
| `features/appbuilder/lib/__tests__/getUiParameterRefs.test.ts` | Removed from PR — layout walker not needed for WebMCP | Removed |

### Submodule `src/shared/entities/parameter/lib/`

| File | Action | Status |
|------|--------|--------|
| `parametersFilter.ts` | `invalidParameters` on import validation | Done |
| `__tests__/parametersFilter.test.ts` | 3 tests for invalid param messages | Done |

### Submodule `src/shared/features/model-state/`

| File | Action | Status |
|------|--------|--------|
| `config/createModelState.zod.ts` | Canonical Zod: core + image ref + data (shared by webmcp / appbuilder / ecommerce) | Done |
| `config/createModelState.ts` | `ICreateModelStateData = z.infer<…>`; re-exports schemas | Done |
| `config/importModelState.zod.ts` | Canonical Zod: input + `nameMessageSchema` (shared by webmcp / ecommerce) | Done |
| `config/importModelState.ts` | `IImportModelStateData` / result; re-exports schemas | Done |
| `model/useImportModelState.ts` | Pass `invalidParameters` on validation fail | Done |

### Parent repo

| File | Action | Status |
|------|--------|--------|
| `index.html` | `%VITE_WEBMCP_ORIGIN_TRIAL_TOKEN%` meta | Done |
| `.env.example` | Document origin trial token | Done |
| `vite.config.ts` | COOP/COEP on dev + preview servers | Done |
| `public/_headers` | COOP/COEP for static production hosts | Done |
| `package.json` | `eval:webmcp` script | Done |
| `src/shared/pages/appbuilder/AppBuilderPage.tsx` | Wire `useWebMcpTools` | Done |

**Conventions:** No `index.ts` barrel in `webmcp` — direct `@AppBuilderLib/features/webmcp/...` imports.

---

## Runtime requirements

- `.env`: `VITE_WEBMCP_ORIGIN_TRIAL_TOKEN` (gitignored)
- Chrome: `chrome://flags/#enable-webmcp-testing` = Enabled
- `crossOriginIsolated` via COOP/COEP (Vite dev/preview + `public/_headers` for prod)
- Hook `ready` = tools registered ∧ `crossOriginIsolated` ∧ `modelContext` available
- API: `await mc.executeTool(tool, JSON.stringify(input))` — second arg **must** be JSON string

---

## Completed tasks (summary)

### Task 1: Scaffold + schemas — Done

- [x] FSD slice `features/webmcp` (config / lib / model / evals / `__tests__`)
- [x] Zod input/output for 4 tools
- [x] `zodToJsonSchema` + `webmcpAvailability`

### Task 2: Tool logic — Done

- [x] `parameterDefinitionMapper`, `stringListValue`; visible filter = store `!definition.hidden`
- [x] `useWebMcpTools` — register 4 tools, `resolveAndUpdate` feedback
- [x] Wire into `AppBuilderPage.tsx`

### Task 3: Origin trial + Vite — Done

- [x] Meta tag + env var + COOP/COEP + empty-token guard

### Task 4: Tests + evals — Done

- [x] Jest: schemas, mapper, set feedback, zodToJsonSchema, `parametersFilter`
- [x] `evals.json` 25 scenarios + `pnpm run eval:webmcp`
- [x] `parametersFilter.test.ts` for `invalidParameters`

### Task 5: QA hardening — Done

- [x] Strict schemas (`z.strictObject`, `additionalProperties: false`)
- [x] Expanded tool descriptions + field `.describe()`
- [x] List execute throws on parse error (no silent `{ parameters: [] }`)
- [x] Bug fixes: StringList string index, visible fallback, navigator `modelContext`

### Task 6: Weak-model QA — Done

- [x] Subagent runs (not `eval:webmcp:gemini` — reverted)
- [x] Gemini 2.5 Flash, GPT-5.4 nano, Haiku 4.5 on complex SS-8076 workflow
- [x] Acceptance: descriptions 4–5/5; 12/12 or equivalent on complex scenario

### Task 7: `invalidParameters` — Done

- [x] `parametersFilter` → `useImportModelState` → `useWebMcpTools`
- [x] Haiku regression 7/7 post-change

### Task 8: Code-review hardening — Done

- [x] **set + sessionId:** `resolveAndUpdate` uses `getParameters(ns)` per update namespace
- [x] **filter visible:** briefly explored layout refs; **final:** store `!definition.hidden` only (no layout dependency)
- [x] **import partial success:** `invalidParameters` on success when some params skipped
- [x] **appliedParameterIds:** empty array when no diff (not all ids)
- [x] **COOP/COEP prod:** `public/_headers` + Vite `preview.headers`
- [x] **error contract:** `formatToolInputError` — list/set return `{ errors: [{ name: '*', message }] }` on parse fail
- [x] **settable flag:** list all params with `settable: boolean`; set rejects unsupported types
- [x] **ready vs registered:** `useWebMcpTools` returns `{ registered, ready, environment }`
- [x] **parametersFilter ripple:** `generateParameterFeedback` prefers `invalidParameters` messages
- [x] Tests: 46 unit + 25 evals

**Skipped (explicit):** silent import (no toasts/URL); unbounded `create_model_state` `data`

### Task 9: PR #334 review follow-ups — Done

- [x] **eval fixtures:** remove `createIsValid`; default mock `isValid: () => true`; inline lambdas only on `intParameter` / `stringListParameter` for rejection evals
- [x] **visible filter:** store-based `!definition.hidden`; dropped layout-ref dependency from webmcp list tool
- [x] **layout walker reverted:** `getParameterRefs` / `useAgent` / `AppBuilderWidgetType` const left as on `development` — not required for WebMCP (PR cleanup)
- [x] **dedupe removed:** branch-introduced `dedupeParameterRefs` deleted with the reverted walker work
- [x] **set validators (final):** single `prepareParameterStoreValue` — shape Color/StringList input, then `parameter.actions.isValid` as the gate; agent-friendly error messages built from `parameter.definition` metadata (type, choices, range). For unknown/unsupported types, surfaces the canonical validator message via `isValid(value, true)` try/catch instead of an empty hint. Per-type validator files removed. Combines PR-review structure with weak-model-friendly messages.
- [x] **canonical create Zod:** `features/model-state/config/createModelState.zod.ts` — `createModelStateCoreSchema` / `createModelStateDataSchema` with field `.describe()`; webmcp = `omit({ image })` only; `appbuildertypecheck` extends core + action messages; `ecommerceapitypecheck` aliases data schema. Image-ref Zod still dual (`createModelStateImageRefSchema` ≈ `IAppBuilderImageRefSchema`) — FSD boundary; shared later if drift. Hand-written `IAppBuilderActionPropsCreateModelState` interface left as pre-existing App Builder pattern.
- [x] **canonical import Zod:** `features/model-state/config/importModelState.zod.ts` — `importModelStateDataSchema` + `nameMessageSchema`; webmcp/ecommerce alias; `IImportModelStateData = z.infer`. Dead WebMCP `export type` / unused create-import output schemas removed.

**Reply-only (no code):** `getParameterRefs` reads accordion layout JSON for in-app `useAgent` `ref.overrides` — unrelated to WebMCP visibility.

---

## Tool contracts (current)

### `list_parameter_definitions`

- Input: `{ filter?: 'all' | 'visible', sessionId?: string }` — not `visibleOnly`
- Output: `{ parameters: [...], errors?: [{ name, message }] }` — each item has `settable: boolean`
- `filter=visible`: parameters where `!definition.hidden` (store-based, not layout)
- `filter=all`: every session param; unsupported types listed with `settable: false`

### `set_parameter_values`

- Input: `{ updates: [{ name, value, sessionId?: string }] }` — not `parameters` / `id`
- StringList: 0-based integer index only
- Output: `{ applied: string[], errors: [{ name, message }] }` — parse errors use `name: "*"`
- `sessionId` per update resolves lookup + write in same namespace

### `create_model_state`

- Success: `{ success: true, modelStateId, modelViewUrl, ... }`
- Failure: `{ success: false, error }`

### `import_model_state`

- Success: `{ success: true, appliedParameterIds, invalidParameters?: [...] }` — `appliedParameterIds` = ids that changed; `[]` if none; `invalidParameters` when partial import skipped params
- Failure: `{ success: false, message, invalidParameters?: [{ name, message }] }` on total validation fail (empty on fetch errors)

---

## Bugs fixed during QA

| Bug | Fix |
|-----|-----|
| `modelContext` false on Chrome 149 | `navigator.modelContext` fallback |
| `crossOriginIsolated` false | COOP/COEP in vite |
| StringList set fails | `toStringListStoreValue` → string index |
| `list visible` = 0 | store `!definition.hidden` (layout refs no longer used by webmcp list) |
| per-type set validators duplicated `isValid` | single `prepareParameterStoreValue` — `isValid` gate + custom messages from `definition` metadata + canonical `isValid(value, true)` fallback for unknown types |
| eval `createIsValid` duplicated validation | per-mock inline `isValid` only where eval rejects |
| set `sessionId` mismatch | `getParameters(ns)` per update |
| misleading `appliedParameterIds` | `computeAppliedParameterIds`; `[]` when no diff |
| list/set parse error shape | `formatToolInputError` |
| unsupported types hidden | `settable: false` on list; set rejects |
| `registered` without COI | `ready` + `environment` on hook |
| `visibleOnly` silent all | `strictObject` + list throws |
| Weak models use `parameters`/`id` | strict schema + `unrecognized_keys` errors |

---

## Verification

```bash
pnpm test -- parametersFilter webmcp   # 46/46
pnpm run eval:webmcp                    # 25/25
pnpm exec tsc --noEmit
```

Subagent / manual (Chrome + origin trial + flag):

1. `getTools()` → 4 tools
2. `executeTool(tool, JSON.stringify({ filter: 'all' }))`
3. SS-8076: set → create → reset → import → verify `currentValue`
4. Negatives: `visibleOnly`, `parameters`/`id`, StringList labels

---

## Follow-ups (deferred — TODO in code)

| Item | Location | Decision |
|------|----------|----------|
| Grasshopper-dev-controlled param/type exposure | `listParameterDefinitions.ts:62` | New ticket — GH metadata contract |
| Browser create/import roundtrip evals | `runWebmcpEvals.ts:209` | Deferred — subagent QA + schema evals sufficient; Playwright e2e optional |
| Export/action WebMCP tools | — | Ticket out of scope |
| Dynamic `_appbuilder` namespace params | `useAgent.ts` parity | Out of scope |

### Browser evals — why deferred

`eval:webmcp` validates create/import **input schema only** (`schema-valid (mocked execution)`). Full roundtrip needs live Chrome + ShapeDiver API. `eval:webmcp:gemini` was reverted; Cursor subagents + Chrome DevTools MCP used instead.

---

## Git workflow

- Branch `task/SS-9745`, commits `SS-9745: ...`
- Submodule `src/shared` + parent pointer bump
- Do not commit `.env`
- PR by user only
